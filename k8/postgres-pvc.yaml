apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: cohort
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/part-of: cohort-platform
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
