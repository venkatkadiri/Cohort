apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: cohort
  labels:
    app.kubernetes.io/name: web
    app.kubernetes.io/part-of: cohort-platform
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3000
      targetPort: 3000
  selector:
    app.kubernetes.io/name: web
