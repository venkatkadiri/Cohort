apiVersion: v1
kind: Service
metadata:
  name: temporal-ui
  namespace: cohort
  labels:
    app.kubernetes.io/name: temporal-ui
    app.kubernetes.io/part-of: cohort-platform
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  selector:
    app.kubernetes.io/name: temporal-ui
