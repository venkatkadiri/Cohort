apiVersion: v1
kind: Service
metadata:
  name: temporal
  namespace: cohort
  labels:
    app.kubernetes.io/name: temporal
    app.kubernetes.io/part-of: cohort-platform
spec:
  type: ClusterIP
  ports:
    - name: grpc
      port: 7233
      targetPort: 7233
  selector:
    app.kubernetes.io/name: temporal
