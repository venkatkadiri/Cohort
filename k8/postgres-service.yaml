apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: cohort
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/part-of: cohort-platform
spec:
  type: ClusterIP
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  selector:
    app.kubernetes.io/name: postgres
