apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cohort-dev-eu-west1
  namespace: argocd
  labels:
    app.kubernetes.io/name: cohort-platform
    app.kubernetes.io/part-of: cohort
    environment: dev-eu-west1
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: cohort-platform
  source:
    repoURL: 'https://github.com/venkatkadiri/Cohort.git'
    targetRevision: HEAD
    path: helm/cohort-platform
    helm:
      valueFiles:
        - values-dev-eu-west1.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: cohort-dev-eu-west1
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
      - ServerSideApply=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
