apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "configuration-hub.fullname" . }}
  labels:
    {{- include "configuration-hub.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "configuration-hub.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "configuration-hub.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: configuration-hub
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8006
              name: http
            - containerPort: 50056
              name: grpc
          envFrom:
            - configMapRef:
                name: {{ include "configuration-hub.fullname" . }}-config
          readinessProbe:
            httpGet:
              path: {{ .Values.probes.readinessPath }}
              port: 8006
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: {{ .Values.probes.livenessPath }}
              port: 8006
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
