apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "booking-hub.fullname" . }}
  labels:
    {{- include "booking-hub.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "booking-hub.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "booking-hub.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: booking-hub
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8004
              name: http
            - containerPort: 50051
              name: grpc
          envFrom:
            - configMapRef:
                name: {{ include "booking-hub.fullname" . }}-config
          readinessProbe:
            httpGet:
              path: {{ .Values.probes.readinessPath }}
              port: 8004
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: {{ .Values.probes.livenessPath }}
              port: 8004
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
