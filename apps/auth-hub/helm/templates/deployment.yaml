apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "auth-hub.fullname" . }}
  labels:
    {{- include "auth-hub.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "auth-hub.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "auth-hub.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: auth-hub
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8003
              name: http
            - containerPort: 50054
              name: grpc
          envFrom:
            - configMapRef:
                name: {{ include "auth-hub.fullname" . }}-config
          readinessProbe:
            httpGet:
              path: {{ .Values.probes.readinessPath }}
              port: 8003
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: {{ .Values.probes.livenessPath }}
              port: 8003
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
