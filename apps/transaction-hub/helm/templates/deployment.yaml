apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "transaction-hub.fullname" . }}
  labels:
    {{- include "transaction-hub.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "transaction-hub.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "transaction-hub.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: transaction-hub
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8008
              name: http
          envFrom:
            - configMapRef:
                name: {{ include "transaction-hub.fullname" . }}-config
          readinessProbe:
            httpGet:
              path: {{ .Values.probes.readinessPath }}
              port: 8008
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: {{ .Values.probes.livenessPath }}
              port: 8008
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
