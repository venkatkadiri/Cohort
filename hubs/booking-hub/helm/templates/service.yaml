apiVersion: v1
kind: Service
metadata:
  name: {{ include "booking-hub.fullname" . }}
  labels:
    {{- include "booking-hub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8004
      name: http
      protocol: TCP
    - port: {{ .Values.service.grpcPort }}
      targetPort: 50051
      name: grpc
      protocol: TCP
  selector:
    {{- include "booking-hub.selectorLabels" . | nindent 4 }}
