apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "leader-hub.fullname" . }}
  labels:
    {{- include "leader-hub.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "leader-hub.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "leader-hub.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: leader-hub
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8007
              name: http
          envFrom:
            - configMapRef:
                name: {{ include "leader-hub.fullname" . }}-config
          readinessProbe:
            httpGet:
              path: {{ .Values.probes.readinessPath }}
              port: 8007
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: {{ .Values.probes.livenessPath }}
              port: 8007
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
